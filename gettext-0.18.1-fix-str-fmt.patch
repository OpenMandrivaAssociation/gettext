--- gettext-0.18.2/gettext-tools/gnulib-tests/test-xvasprintf.c.str~	2012-12-25 06:29:46.000000000 +0100
+++ gettext-0.18.2/gettext-tools/gnulib-tests/test-xvasprintf.c	2012-12-26 18:52:56.049268778 +0100
@@ -56,7 +56,7 @@ test_xvasprintf (void)
   {
     /* Silence gcc warning about zero-length format string.  */
     const char *empty = "";
-    result = my_xasprintf (empty);
+    result = my_xasprintf ("%s", empty);
     ASSERT (result != NULL);
     ASSERT (strcmp (result, "") == 0);
     free (result);
@@ -95,7 +95,7 @@ test_xasprintf (void)
   {
     /* Silence gcc warning about zero-length format string.  */
     const char *empty = "";
-    result = xasprintf (empty);
+    result = xasprintf ("%s", empty);
     ASSERT (result != NULL);
     ASSERT (strcmp (result, "") == 0);
     free (result);
--- gettext-0.18.2/gettext-tools/libgrep/m-fgrep.c.str~	2012-12-04 07:28:58.000000000 +0100
+++ gettext-0.18.2/gettext-tools/libgrep/m-fgrep.c	2012-12-26 18:52:03.449904455 +0100
@@ -106,7 +106,7 @@ Fcompile (const char *pattern, size_t pa
       for (lim = beg; lim < pattern + pattern_size && *lim != '\n'; ++lim)
         ;
       if ((err = kwsincr (ckwset->kwset, beg, lim - beg)) != NULL)
-        error (exit_failure, 0, err);
+        error (exit_failure, 0, "%s", err);
       if (lim < pattern + pattern_size)
         ++lim;
       beg = lim;
@@ -114,7 +114,7 @@ Fcompile (const char *pattern, size_t pa
   while (beg < pattern + pattern_size);
 
   if ((err = kwsprep (ckwset->kwset)) != NULL)
-    error (exit_failure, 0, err);
+    error (exit_failure, 0, "%s", err);
   return ckwset;
 }
 
--- gettext-0.18.2/gettext-tools/libgrep/m-regex.c.str~	2012-12-04 07:28:58.000000000 +0100
+++ gettext-0.18.2/gettext-tools/libgrep/m-regex.c	2012-12-26 18:52:03.449904455 +0100
@@ -106,7 +106,7 @@ compile (const char *pattern, size_t pat
 
         if ((err = re_compile_pattern (motif, len,
                                        &cregex->patterns[cregex->pcount].regexbuf)) != NULL)
-          error (exit_failure, 0, err);
+          error (exit_failure, 0, "%s", err);
         cregex->pcount++;
 
         motif = sep;
